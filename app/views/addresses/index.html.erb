<% provide(:title, 'Sign up') %>
<h1>住所登録変更</h1>

<h2>登録住所一覧</h2>
<% @addresses.each do |address| %>
  <ul>
    <%= address.address %>
    <%= link_to "削除", address,  method: :delete, data: { confirm: '住所を削除してもよろしいですか？'}%>
  </ul>
<% end %>

<h3><%= link_to "新規住所登録", new_address_path %></h3>
